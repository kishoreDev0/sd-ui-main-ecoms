// Api Versions
export enum ApiVersions {
  V1 = '/api/v1',
}


export enum Products{
    PRODUCTS = '/product',
    PRODUCT = '/products',
}
export enum Feature{
    FEATURES = '/features',
    FEATURE = '/feature',
}